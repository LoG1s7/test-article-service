meta:
   environment: python
   toolchain:
     name: pip
     version: 3.10

   build:
   requirementsPath: article_service/requirements.txt
   run:
    command: cd article_service && gunicorn article_service.wsgi:application --bind 0.0.0.0:80
    persistenceMount: /data
    containerPort: 80
